name: CommentJira

on:
  push:
    branches-ignore:
      - '**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Comment on issue
      - uses: atlassian/gajira-comment@master
        with:
          issue: DC-3843
          comment: CommentJira
